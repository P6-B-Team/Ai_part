name: CI - Build AI Service

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./02_AI_Service
    steps:
      - uses: actions/checkout@v4
      - name: Build Docker image
        run: docker build -t wst-ai-service .
